    // bool isDeadEnd(int _x,int _y, pose pos){
    //     bool deadEnd = false;
    //     switch (pos){
    //         case right:
    //             deadEnd = isCorridor(int _x,int _y, pose pos) && !( maze.cell(_x,_y+1));
    //             break;
    //         case left:
    //             deadEnd = isCorridor(int _x,int _y, pose pos) && !( maze.cell(_x,_y-1));
    //                 break;
    //         case up:
    //             deadEnd = isCorridor(int _x,int _y, pose pos) && !( maze.cell(_x-1,_y));
    //                 break;
    //         case down:
    //             deadEnd =  isCorridor(int _x,int _y, pose pos) && !( maze.cell(_x+1,_y));
    //             break;
    //         default:

    //     }
    // }